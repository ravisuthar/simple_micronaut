 schema {
    query: Query
    mutation: Mutation
 }

type Query {
    hello(name: String): String!
    user: User
}

type Mutation{
	addUser(user: String!): Boolean
}

type User{
	id: String
	name: String
	email: String
}